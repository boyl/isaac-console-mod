# Isaac Console Mod 仓库规则

## 唯一源码入口

- 本仓库是“以撒中文控制台”和英文版 `Console UI` 的唯一正式源码入口。
- 今后本项目的 Isaac Mod 代码、测试、版本、文档、打包和发布迭代，必须先在本仓库完成并形成可追溯 Git 差异；此规则不适用于其他独立 Isaac Mod 项目或其他游戏的 Mod。
- 中文版源码位于 `workshop-mod/`，英文版源码位于 `workshop-mod-en/`，共同测试位于 `tests/workshop-mod/`，允许列表构建入口为 `tools/build-workshop-mod.ps1`。
- 桌面版控制台仓库与本仓库职责分离；不得把 Mod 源码、测试或构建产物放回桌面版仓库。

## 禁止反向开发

- `outputs/`、`dist/`、游戏 `mods` 安装目录和 Steam Workshop 订阅副本只能用于构建、安装、验收、取证或恢复，不得作为日常编辑源。
- 若安装副本或远端订阅副本含有仓库尚未记录的正确改动，先记录来源、版本、文件树和 SHA-256，与仓库逐文件比较，再把明确差异回迁到本仓库并验证；不得直接在副本上继续迭代。
- 不得用开发目录中的陈旧或占位 `preview.png` 覆盖远端正确预览图。构建必须通过脚本中的预览图哈希门禁。

## 修改与验证

- 修改前核对仓库 Owner、`.git` Owner、分支、HEAD、工作树状态和远端；保留用户已有未提交改动。
- 保留中英文各自的 Workshop ID、`RegisterMod` 名称、目录标识符和 SaveData 身份。
- 行为修改同步更新中英文实现和适用测试；只有确有语言或运行时差异时才允许分叉。
- 测试必须产生真实场景日志或明确断言结果；只有退出码而没有预期执行证据，不得报告通过。
- 从源码运行回归，再用 `tools/build-workshop-mod.ps1` 生成 `dist/workshop-candidates/` 允许列表候选；不得把源码目录直接当作上传包。
- 分别记录源码验证、候选验证、安装验证、游戏运行时验证、用户验收和远端发布状态。

## 安装与发布

- 本地安装只能从已验证候选进行，并逐文件核对哈希；不要从源码目录或旧输出目录安装。
- 未经用户明确要求，不配置或修改 Git 远端，不推送，不上传 Workshop。
- 发布前后都要验证 Workshop ID、metadata 版本、远端预览图、订阅副本和公开页面；上传失败或远端未复核时不得声称已发布。
