# 高清字体发布记录

## 最终要求

高清字体作为内置设置，默认关闭。用户已实机确认保持原字号和布局的方案，并于 2026-09-16 明确授权发布。

- 标题／正文／辅助文字为 12／10／10 个布局单位，思源黑体 Medium／Regular 以 24／20／20 像素生成灰度抗锯齿 RGBA 图集，绘制缩放为 0.5。
- 经典与高清均保持两列、每页 8 项、分类每页 6 项；控件几何沿用经典度量，文字测量、截断、换行和绘制使用当前字体。字体切换不重排容器。
- 增量保存 hdFontEnabled=0/1；无字段时关闭。保留搜索、页码、选中项、命令草稿、收藏和历史；切换不执行命令。
- 高清资源按需加载。失败时当前会话降级、提示一次并记日志，不覆盖偏好；动态缺字由原字体补绘。
- 不新增原生插件依赖，不修改游戏渲染配置，不改 SaveData 身份。MCM 与此前闪退排查不在本次范围内。

## 验收证据

| 范围 | 结果 |
|---|---|
| 双语源码 | 220 个 Mock + 12 个冷启动／生命周期场景通过 |
| 高清切换 | 双语 × Repentance/Rep+ API Mock × 加载成功/失败，共 8 场景通过；高清开启与关闭后逐项比对经典布局所有字段 |
| 旧版数据 | 实际备份中英文旧代码可读取新增字段，2 场景通过 |
| 字体资产 | 每档 7544 字形、6／5／5 张图集；BMFont、哈希、页面边界、RGBA 和灰度检查通过 |
| 中文实机 | Repentance+ v1.9.7.17，用户确认等尺寸外观；重启记忆正常，往返均保持设置第 2 页、第 2 项、容量 8 |
| 英文实机 | 同一运行时，默认关闭、等尺寸外观与往返通过 |
| Repentance 实机 | 中英文均完成真实启动、字体加载、等尺寸显示和零截图开关往返；页码、选中项、容量及存档不变。中文重启记忆正常。证据 hd-live-toggle-zh-rep-v11 / hd-live-toggle-en-rep-v12 |
| 实机批处理 | 中英文往返均为 PASS、0 截图；SaveData 字节及 options.ini 字节恢复一致 |
| 实机字体故障 | 英文标题字体临时缺失时菜单仍可打开并使用经典字体，日志明确 title font missing，偏好仍为 1；随后按哈希恢复文件 |
| 手柄 | 用户反馈功能正常；指出原大字号布局问题后，已改成等尺寸并得到确认 |
| 回退 | 默认预览、后续编辑或删除停止、备份损坏停止、路径穿越停止、恢复与幂等、存档保留均通过 |

实机截图是 2442×1304，游戏布局为 488×260，不标为精确 1440p、1080p 或 4K 通过。Repentance 补测截图为 2258×1286、布局 451×257。精确多分辨率和全部设备／搜索／命令组合尚未全面实机验证；用户在获知这些限制后授权发布，不把该授权写成测试通过。

最终发布仅在验收实现上更新版本和设置说明。历史过程中的 18／14／12 重排方案已撤销。

## 构建与工具

来源固定提交与哈希见 tools/hd-font-source.json；使用 tools/fetch-hd-fonts.ps1、tools/build_hd_fonts.py 构建，依赖版本见 tools/requirements-hd-fonts.txt，字体携带 OFL 许可证。

源码验证入口为 tests/workshop-mod/run_all_tests.py、run_hd_menu_tests.py、test_hd_font_assets.py、test_hd_rollback.py；自动化自测为 hd-automation.test.mjs。允许列表构建使用 tools/build-workshop-mod.ps1，正式发布使用 tools/publish-workshop.ps1。

tools/open-isaac.ps1 自动定位 Steam 并启动或复用游戏。tools/acceptance/open-isaac.mjs 与 hd-menu.mjs 为 Codex sky 批处理入口；正常操作读取日志而不逐步截图，未知状态停止。鼠标模板仅支持本次校准的 Rep+ 2442×1304 和 Rep 2258×1286 窗口。

## 回退与数据

准确基线备份包含实际中英文安装、源码、控制台自身设置、文件清单和 SHA-256。测试结束后原安装及启用状态均已恢复；没有恢复整个游戏存档。中文保留用户选择的高清开启，英文恢复测试前关闭，这不改变新用户默认关闭。

install-hd-menu.ps1 默认预览，安装前要求游戏退出并验证准确基线。每次安装使用新的恢复目录，保存 before/ 与 rollback.json。恢复执行：

```powershell
pwsh -File restore-hd-menu.ps1 -Manifest ./rollback.json
pwsh -File restore-hd-menu.ps1 -Manifest ./rollback.json -Apply
```

恢复前核对路径、Mod 身份、备份和候选哈希；后续修改或删除时停止并列出差异。只恢复本次涉及的文件，只移除清单内本次新增且仍匹配的文件，不清空 Mod 目录。默认保留收藏、历史、自定义命令和设置。

备份、脚本和日志不进入 Workshop 包。Steam 自动更新可能覆盖本地回退；已发布版本如需撤回，须对同一项目再发布修复版本。源码保留用户原有未提交测试改动，发布时与本次回归改动一并记录。
